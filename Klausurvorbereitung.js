/*Probeklausur Peter Schmidt

Aufgabe 1
Erzeugen Sie ein Array testArr mit 4 beliebigen Elementen vom Datentyp String.
Geben Sie anschließend das letzte Element in die JS-Konsole aus. 
*/
let testArr = ["string1", "string2", "string3", "string4"];

    for (let index = 0; index < testArr.length; index++) {

        const element = array[index];

        console.log(testArr)
       
    }

console.log(testArr[testArr.length-1]); 

/* let ist das Schlüsselwort um das Array zu initialisieren, danach wird der bezeichner festgelegt und nach dem zuweisungsoperator = in [] die Array Elemente zugewiesen
Danach Erfolgt die Anweisung, das letze Element des Arrays in der Konsole a

